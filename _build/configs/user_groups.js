/**
 * Свои группы пользователей пакета.
 *
 * TotalLog их НЕ создаёт: админ чужого сайта лишним группам не обрадуется, а доступ
 * закрывается разрешениями MODX — см. permissions.js (шаблон разрешений + политики,
 * которые админ вешает на свои существующие группы).
 *
 * Механизм оставлен для случаев, когда пакету действительно нужна собственная группа
 * (закрытый раздел, куда админ просто добавляет людей). Формат:
 *
 * export default {
 *     'MyGroup': {
 *         description: 'Доступ к разделу',
 *         resource_groups: ['my_group_pages'],  // группы ресурсов из resource_groups.js
 *         policy: 'Resource',                   // политика доступа (по умолчанию Resource)
 *         context: 'web',
 *         authority: 9999,
 *     },
 * }
 */
export default {}
