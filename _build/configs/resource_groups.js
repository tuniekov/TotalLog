/**
 * Групп ресурсов пакет не создаёт: доступ к данным закрыт разрешениями MODX
 * (см. permissions.js + `permissions` в gtsapipackages.js), а сами страницы —
 * обычные опубликованные ресурсы. Меньше сущностей на чужом сайте.
 */
export default {}
