export default {
    // Журнал в админке MODX — тот же модуль TLAll, что и на странице сайта.
    //
    // Своего контроллера у TotalLog нет и не нужно: пункт ведёт на страницу
    // gtsAPI (namespace 'gtsapi', action 'home'), а она по &config поднимает
    // приложение. Приложение открывается в iframe, поэтому стили менеджера
    // и Tailwind внутри журнала не мешают друг другу.
    //
    // Пункт админский: показывает все запросы и все поля. Пользовательский
    // журнал живёт страницей сайта (resources.js) — туда ходят не через менеджер.
    'Журнал запросов': {
        description: 'Кто, что и когда делал: все запросы MODX',
        action: 'home',
        namespace: 'gtsapi',
        // Только имя системной настройки: JSON прямо в params ломает меню
        // менеджера — оно проходит через Smarty, и { } он разбирает как свой тег.
        params: '&config=totallog_admin',
        parent: 'components',
        menuindex: 0,
    },
}
